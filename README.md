# fc

Проект: **C++14 + Qt5 + qmake (.pro), Windows**.

Добавлен шаблон `config.ini` в корень репозитория.

> Сейчас приложение использует параметры, зашитые в коде (`mainwindow.cpp`).
> `config.ini` подготовлен как единая точка конфигурации (шаблон), чтобы удобно
> перенести параметры в чтение из файла на следующем шаге.
